# Secrets and environment files. Copy each .env.example to .env locally; your
# real .env stays out of Git. The .env.example templates remain tracked because
# they are a different filename.
.env

# Ansible Vault: never commit the vault password. The encrypted vars/secrets.yml
# is safe to commit because its contents are encrypted.
.vault_pass
vault_pass.txt
*.retry

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Build artefacts (the Week 3 wheel and its packaging output)
build/
dist/
*.egg-info/
report.xml

# Terraform (Weeks 4 and 5). The lock file .terraform.lock.hcl stays committed.
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Generated by the Week 5 integrated lab
**/inventory/terraform_outputs.json
**/inventory/inventory.ini

# OS and editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
