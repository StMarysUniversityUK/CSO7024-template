# Week 4 starter: Infrastructure as Code

- `main.tf` is the starting configuration for lab 4.2 (local and random providers).
- `scripts/start-state-backend.sh` is the local state backend for lab 4.3.

Work in `~/devops-week4`. Copy these files there, or use them in place.

No cloud account is required: everything runs on your own machine.
