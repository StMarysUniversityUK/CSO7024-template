<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ site_title }}</title>
</head>
<body>
  <h1>{{ site_title }}</h1>
  <p>This page was deployed by Ansible to {{ inventory_hostname }}.</p>

  {# TODO (lab 5.3, Step 5): once vars/secrets.yml is loaded, show the admin
     contact from the vault-encrypted variable, for example:
     <p>Administered by {{ site_admin_email }}.</p>
  #}
</body>
</html>
