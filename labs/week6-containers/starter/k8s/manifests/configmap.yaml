# configmap.yaml — non-secret configuration for the app (lab 6.3).
# Fill in the TODO values, then reference this from deployment.yaml.

apiVersion: v1
kind: ConfigMap
metadata:
  name: cso7024-app-config
  labels:
    app: cso7024-app
data:
  # TODO (Step 3): provide the app's configuration here, then wire it into the
  # Deployment with envFrom. The app reads PORT and GREETING from the
  # environment.
  PORT: "8000"
  GREETING: "Hello from Kubernetes"
